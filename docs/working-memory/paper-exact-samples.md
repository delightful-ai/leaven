# Paper-Exact One-Sample Lanes

Status: first sample-materialization pass complete.

Generated bytes live under ignored `tmp/`; this note and the scripts are the
tracked handoff surface.

## Commands

```bash
python3 scripts/materialize_paper_exact_samples.py
python3 scripts/build_paper_exact_lanes.py
```

Primary generated manifests:

- `tmp/paper_exact_samples/manifest.json`
- `tmp/paper_exact_lanes/manifest.json`

Rendered preflights:

- `tmp/paper_exact_lanes/evoskill/rendered_officeqa_prompt.md`
- `tmp/paper_exact_lanes/trace2skill/rendered_case_prompt.md`
- `tmp/paper_exact_lanes/memento-skills/gated_access_preflight.md`
- `tmp/paper_exact_lanes/d2skill/rendered_alfworld_prompt.md`
- `tmp/paper_exact_lanes/skillreducer/rendered_reduction_prompt.md`

## Samples

EvoSkill:

- OfficeQA Pro case `UID0001` materialized from
  `tmp/repros/officeqa/officeqa_pro.csv`.
- The referenced Treasury Bulletin source text
  `treasury_bulletin_1941_01.txt` was extracted from the official transformed
  OfficeQA zip.
- SealQA `seal_0` first streamed case materialized from
  `hf://datasets/vtllms/sealqa/seal_0/test`.

Trace2Skill:

- SpreadsheetBench Verified case `13-1` materialized from the upstream
  `spreadsheetbench_verified_400` checkout.
- The lane includes `prompt.txt`, the initial xlsx, and the golden xlsx.

Memento-Skills:

- GAIA and HLE did not materialize in this machine's current Hugging Face auth
  state. Both returned gated-dataset errors, and access-blocked reports were
  written under `tmp/paper_exact_samples/memento-skills/`.
- Do not substitute another dataset and call the lane exact. Either accept the
  HF gates / provide credentials or explicitly approve a non-exact surrogate.

D2Skill:

- ALFWorld official `json_2.1.1_json.zip` was downloaded from the ALFWorld
  GitHub release.
- The first train `traj_data.json` was extracted for a one-trajectory lane.
- The D2Skill ALFWorld prompt source and run scripts are copied into the lane.

SkillReducer:

- SkillsBench was cloned under ignored `tmp/repros/skillsbench`.
- The lane uses task `jax-computing-basics` plus the real
  `.agents/skills/skill-creator/SKILL.md` skill fixture.

## Exactness State

The lanes are not yet full live 1:1 replications. They are the necessary
materialized substrate for that work:

- real one-sample datasets are present for EvoSkill, Trace2Skill, D2Skill, and
  SkillReducer;
- prompt/setup source files are copied into per-paper lane reports;
- prompt/setup preflights are rendered from those samples where possible;
- Memento-Skills is honestly blocked on gated GAIA/HLE access;
- Codex with `gpt-5.4-mini` remains the intentional model substitution.

Next implementation pass should adapt the per-paper runners to consume these
`tmp/paper_exact_lanes/*/lane.json` reports, starting with Trace2Skill and
EvoSkill because their real samples and prompt surfaces are most concrete.
