# GEPA Upstream Parity Matrix

Status: active execution matrix.
Date: 2026-05-17.

This is the working proof surface for the GEPA parity goal. It is not product
law. Product law remains in `docs/specs/initial_library.md`,
`docs/specs/gepa_reference_behavior.md`, and
`docs/specs/gepa_aime_paper_parity.md`.

Rules for this matrix:

- Every row needs an upstream anchor, a Leaven path, a proof, and a verdict.
- Verdicts are `proven`, `gap`, `intentional-delta`, or `unknown`.
- A Leaven-better behavior still needs tests and report/docs disclosure.
- Do not use deterministic P8 smoke, fake reflectors, or topology-only checks
  as proof for live GEPA/AIME parity.

Audit snapshot for this pass: current Leaven worktree;
`~/vendor/github.com/gepa-ai/gepa` at
`ce51b50cd196b539c25fae99ad0e0255c23004a4`;
`~/vendor/github.com/stanfordnlp/dspy` at
`ddb09e7dd42676409574aa8e6a14c1d05161658a`. These vendor heads are newer than
the verified hashes named in `docs/specs/gepa_reference_behavior.md`; this
matrix records current audit anchors and does not repin the spec.

## Matrix

| Requirement | Upstream anchor | Leaven path | Current proof | Verdict | Next action |
| --- | --- | --- | --- | --- | --- |
| Core reference loop defaults are explicit: validation-frontier parent selection, full validation, epoch minibatch 3, round-robin part selection, strict acceptance, skip-perfect, merge off | upstream `src/gepa/api.py::optimize`, `src/gepa/strategies/*`; spec section 3.1 | `crates/leaven-gepa/src/builder.rs`, `src/optimizer.rs`, `src/validation.rs`, `src/selector.rs` | `gepa_default_validation_policy_is_full_validation`, `public_reference_builder_requires_surface_then_reflector`, `gepa_default_max_iterations_is_not_one_iteration_smoke_config`; fallback selector is named `PopulationBestFallback` and absent from `leaven_gepa::prelude` | proven | Keep selector internals out of ordinary GEPA examples; advanced root exports remain ablation/customizer surface. |
| Public Layer 1 route optimizes through `optimize(seed).train().validation().test().runner().score().using(Gepa...).run()` without engine internals | upstream AIME quickstart `examples/aime_math/main.py`; Leaven AIME spec section 1 | `crates/leaven-run`, `examples/p8_aime_gepa/src/main.rs::run_aime` | `deterministic_aime_acceptance_shows_public_gepa_improvement` asserts the typed `Optimized::optimizer_report::<GepaReport>()` from the public path carries best indices, candidates, lineage, validation subscores/frontier, history, attempts, metric calls, skip policy, and events; `run_builder_exposes_typed_optimizer_report_payload` | proven | Keep this as a public-path scenario when report fields move between GEPA and run summary layers. |
| Seed full validation happens before any train minibatch | upstream `src/gepa/core/engine.py::run`; spec sections 4 and 8 | `Gepa::initialize`, `validate_candidate` | `reference_state_seed_validation_initializes_candidate_zero_before_train` | proven | Keep this row covered when changing initialize/checkpoint behavior. |
| Empty validation is refused before evaluator/provider work | upstream core requires `valset`; spec sections 4 and 8 | `FullValidation`, `Gepa::initialize` | `reference_gepa_refuses_empty_validation_before_evaluator_work`, P8 `reference_gepa_requires_validation_instead_of_silent_train_only_fallback` | proven | Keep no train-only fallback in public examples. |
| Seed validation initializes candidate index 0, lineage, validation subscores, frontier membership, and metric-call counters | upstream `GEPAState.initialize_gepa_state`; spec sections 4 and 11 | `crates/leaven-gepa/src/state.rs`, `src/report.rs` | `reference_state_seed_validation_initializes_candidate_zero_before_train`; `GepaReferenceState` unit coverage; public P8 typed-report assertions for seed index, lineage, validation subscores, and frontier | proven | Keep report-level seed assertions on the public optimize path. |
| Reference parent selection samples validation Pareto frontier frequency after dominance pruning with checkpointed RNG | upstream `ParetoCandidateSelector`, `select_program_candidate_from_pareto_front`, `remove_dominated_programs`; GEPA tests `test_candidate_selector.py`, `test_gepa_utils.py` | `GepaReferenceState::select_by_validation_frontier_frequency`, `Gepa::select_reference_parent`, opaque doc-hidden Python-compatible shared `random.Random` randbelow | `default_parent_selection_samples_validation_frontier_frequency`; state frequency, dominance, and RNG-restore tests; `gepa_resume_restores_selector_rng_and_part_cursor_after_accepted_child`; private RNG tests pin seed-0 randbelow sequence | proven | Keep next-parent-after-accepted-validation proof when touching frontier admission. |
| Parent selection precedes train minibatch sampling | upstream `reflective_mutation.py::prepare_proposal`; spec phase order section 4 | `optimizer/step.rs::run_iteration` | `parent_and_child_screen_on_same_ordered_train_cases` asserts parent-selected event precedes train-minibatch event | proven | Fold into broader GEPA phase-order assertion row. |
| Train sampler uses only train/search cases, defaults to minibatch size 3, shares selector RNG, and resumes deterministically | upstream `EpochShuffledBatchSampler`; spec sections 5 and 19.2 | `EpochShuffled`, GEPA checkpoint state, opaque doc-hidden Python-compatible `random.Random` shuffle | `reference_profile_shares_rng_between_parent_selection_and_epoch_sampler` proves parent selection advances the sampler RNG; `epoch_shuffled_samples_train_with_seed_and_restores_cursor` pins seed-41 Python shuffle/padding order; `gepa_default_sampler_uses_train_minibatches_without_validation_or_test_cases`, `gepa_checkpoint_state_includes_validation_policy_state`, `gepa_resume_restores_sampler_cursor_and_does_not_repeat_seed_validation`; private RNG tests pin seed-0/41 shuffle sequences | proven | Keep restore-through-optimizer coverage when changing GEPA checkpoint state. |
| Parent and child screening use the exact same ordered train minibatch | upstream `reflective_mutation.py::execute_proposal`; spec section 19.3 | `run_iteration`, `process_proposal` | proposal-attempt rows carry parent/child cases; `parent_and_child_screen_on_same_ordered_train_cases`; equal-score rejection also asserts parent/child case equality | proven | Keep proposal-attempt case lists in report snapshots. |
| Acceptance is strict improvement on train minibatch only; equal score is rejected without validation/admission | upstream `StrictImprovementAcceptance`; spec section 5 | `StrictImprovement`, `process_proposal` | `StrictImprovement` unit assertions; `strict_equal_score_child_is_rejected_without_full_validation_or_admission` | proven | Add negative proof for policy variants only if they become ordinary public profiles. |
| Accepted child is full-validated before admission to GEPA reference state/frontier | upstream core full-eval/add-candidate path; spec sections 5 and 19.3 | `accept_child`, `validate_candidate`, `GepaReferenceState::add_validated_candidate`, `GepaProposalAttempt::admitted_index` | `accepted_child_enters_reference_state_only_after_full_validation`; `accepted_child_updates_validation_frontier_before_next_parent_selection` proves an admitted child becomes candidate index 1 and the following iteration selects it from the updated validation frontier; `budget_stop_after_train_acceptance_reports_child_not_admitted` proves a direct engine hard-cap failure between train acceptance and validation does not look admitted; public `Budget::metric_calls` now uses GEPA-style pre-step stopping so started accepted-child validation can finish; `train_accepted_child_without_validation_is_not_reference_admitted` proves no-validation scaffold policies keep children train-population-only | proven | Keep next-parent-after-accepted-validation proof when touching frontier admission. |
| Evaluation cache reuses per candidate/case rows and cache hits do not increment GEPA metric calls | upstream `EvaluationCache`; GEPA `test_evaluation_cache.py`; spec sections 18 and 19.4 | engine per-case backfill via `evaluate_casewise`; `leaven-run` cache summaries | `gepa_reuses_evaluation_cache_per_candidate_case_across_different_requests`, `accepted_child_full_validation_reuses_case_cache_hits`, generic run-cache tests, P8 `deterministic_run_dir_resume_restores_gepa_without_repeating_search_work` asserts durable resumed cache hits and zero-cost hit accounting | proven | Keep restored-cache proof when changing run-dir checkpoint/cache boundaries. |
| Solver parse failures follow DSPy adapter fallback before failing a case | upstream `dspy/adapters/chat_adapter.py::ChatAdapter.__call__`, `dspy/adapters/json_adapter.py::JSONAdapter`; live P8 row omitted `[[ ## reasoning ## ]]` but had `[[ ## answer ## ]]` | `examples/p8_aime_gepa/src/main.rs::complete_openai_solver_output` | `aime_solver_parser_requires_all_dspy_output_fields` proves strict ChatAdapter parsing; `aime_solver_json_fallback_request_matches_dspy_json_adapter` and `aime_solver_falls_back_to_json_adapter_after_chat_parse_failure` prove the public solver reruns through JSONAdapter before `answer_parse` failure | proven | Re-run cache-only live probe before paid resume so prior ChatAdapter calls are reused and only missing JSONAdapter fallback rows spend. |
| Cache profile differences are explicit: upstream core default off, upstream AIME profile on, Leaven durable run-dir auto-cache | upstream `api.py cache_evaluation=False`, `examples/aime_math/main.py cache_evaluation=True`; Leaven run spec | `leaven-run` default cache policy, P8 config/report lines | run-dir cache summary tests; `configured_gepa_aime_profile_matches_reference_knobs`; `report_lines_disclose_live_lm_role_cache_and_runtime_truth` | intentional-delta | Never collapse these into one "GEPA default"; every report must print the profile/cache policy used. |
| All-perfect and no-reflective-example skips happen before part selection, dataset building, reflector calls, or provider work | upstream reflective mutation skip gates; spec sections 6 and 19.5 | `propose_candidate`, `GepaSkipReason`, `GepaProposalAttempt` | `no_reflective_examples_skip_before_reflector_provider_work`, `all_scores_perfect_skip_before_part_dataset_or_reflector_work` | proven | Keep P8 report mapping in sync with skip reasons. |
| Reflection prompt/parser snapshots match the claimed generic GEPA and AIME optimize-anything profiles | upstream instruction proposal, `optimize_anything.py`, DSPy ChatAdapter AIME solver | `DefaultReflectionRenderer`, `PlainTextEditParser`, P8 solver/reflection rendering | `default_renderer_uses_gepa_prompt_template_and_config_override`, `default_renderer_and_plain_text_parser_cover_empty_feedback_and_bad_part`, `plain_text_parser_extracts_gepa_fenced_replacement`, `leaven_reflection_prompt_matches_upstream_gepa_instruction_template`, `p8_reflection_uses_upstream_optimize_anything_template`, AIME solver/parser prompt tests | proven | Keep snapshots pinned to the upstream files/commit actually audited. GEPA reflection attempts carry stable attempt-index provider metadata so the LM cache can resume the same attempt without replaying a prior stochastic response for a later identical prompt. |
| Reflective dataset carries target-safe input/output/score/feedback/side-info/trace refs/provenance; hidden targets reach reflection only through scorer feedback | upstream DSPy adapter `make_reflective_dataset`, optimize-anything side-info rendering; specs `case_visibility`, `gepa_reflection_evidence_visibility` | `RunOutput::with_trace`, `Score::with_trace`, `CaseAssessmentEvidence::with_trace`, `GepaReflectiveDataset`, P8 `AimeReflectiveDataset` | generic target-safe projection tests, `scoring_evaluator_reports_per_candidate_cost_for_independent_batches`, `default_reflective_dataset_projects_parse_failures_without_hidden_case_targets`, agent/LM byte-identical example tests, P8 side-info/scorer parse-failure tests, P8 report JSON test asserts non-empty trace refs | proven | Keep generic projection target-safe; keep AIME side-info as an adapter-specific override, not the only proof. DSPy module-local trace selection remains a DSPy-default non-parity row, not a P8/core GEPA claim. |
| Detailed GEPA result/report exposes resolved profile facts, candidates, lineage, validation aggregate/subscores, frontier, metric calls, proposal attempts, skip policy, events, best validation candidate, best outputs, run dir, seed, and cache facts | upstream `GEPAResult.from_state`; spec sections 6, 11, 21 | `GepaReport`, `GepaEventSummary::ProfileResolved`, `Optimized::optimizer_report::<GepaReport>()`, P8 JSON/report lines plus `Optimized`/run summary for run-owned facts | direct GEPA report tests, `fast_certified_profile_keeps_full_validation_admission`, `accepted_iteration_emits_reference_phase_order`, `run_builder_exposes_typed_optimizer_report_payload`, `deterministic_aime_acceptance_shows_public_gepa_improvement`, `p8_aime_json_report_persists_role_and_proof_facts` asserts optimizer-resolved profile label/known minibatch size/serial proposal count/mode/validation policy/certification/skip policy plus JSON/run-summary equivalence for seed, run id, storage, run dir, checkpoint, summary path, best output, cache facts, P8 candidate system-prompt projection, and P8 proposal-attempt train-screen acceptance/admission/child-index/validation plus reflection request/assistant/proposed-text projection | proven | Keep run-owned facts in `Optimized`/summary and GEPA-owned state in `GepaReport`; P8 carries prompt/proposal text projection for AIME audits while generic artifact projection remains a separate design surface. After durable resume, P8 labels missing prompt text as unavailable process-local LM telemetry rather than implying it was never known. |
| Phase events/progress expose GEPA-specific phases without parsing generic engine events | upstream callbacks; spec sections 4, 14, 21 | `GepaEventSummary`, P8 projection | GEPA event tests and P8 event JSON mapping; `accepted_iteration_emits_reference_phase_order` asserts profile, seed validation, seed frontier, iteration, parent selected, minibatch sampled, parent evaluated with score, dataset built, reflection started/completed with source payloads, child built/evaluated with score, train-screen accepted, accepted validation, candidate admitted, frontier update, stop order, and event metric deltas that reconstruct the GEPA report metric total; `p8_aime_json_report_persists_role_and_proof_facts` asserts P8 JSON carries those payloads; `p8_aime_json_uses_checkpointed_gepa_report_events_after_resume` proves P8 top-level JSON uses checkpoint-restored GEPA report events instead of a fresh observer tail | proven | GEPA events expose optimizer phases and score/source payloads; seed and accepted-validation completion events also carry metric-call deltas for report reconstruction. Provider cache status remains in LM role telemetry and evaluation cache report because generic GEPA reflector events are provider-neutral. |
| Durable resume restores optimizer, sampler/selector state, cache, compatibility fingerprints, and does not repeat committed evaluations | upstream state save/load; AIME spec sections 3 and 4 | `GepaCheckpointState`, engine checkpoints, `leaven-run` compatibility manifest | checkpoint state tests; generic run-dir compatibility/resume tests; `gepa_resume_restores_sampler_cursor_and_does_not_repeat_seed_validation` proves sampler cursor plus no repeated seed validation through `Engine::resume`; `gepa_resume_restores_selector_rng_and_part_cursor_after_accepted_child` proves next parent/part/train cases after accepted-child restore match uninterrupted control; P8 `deterministic_run_dir_resume_restores_gepa_without_repeating_search_work` proves the public run-dir path restores GEPA search state/cache without another reflection call; live run `.leaven/release-runs/p8-aime-gepa-20260518-043717` resumed run id `03ee6ad5-3234-4a24-81ac-f17a7358b275` after OpenAI timeouts, reused durable evaluation cache hits, and emitted final reports | proven | Keep future live reports self-contained with top-level live-provider proof, cumulative GEPA events, and durable provider-failure counters. |
| P8/AIME data path discloses source IDs, split roles, counts, cache hash, target safety, and duplicate/collision refusal | upstream `examples/aime_math/utils.py`; AIME spec sections 2 and 7 | `examples/p8_aime_gepa/src/main.rs` dataset cache/lowering/report | deterministic fixture tests; `aime_cache_loading_preserves_train_validation_test_roles`; report-source/case-safety tests; materialized `target/leaven-aime-cache/aime.json` from HF cache: train 45, validation 45, test 30, 120/120 unique source IDs, SHA-256 `0f39c54861fd37a609d5bf397902a2086c245ebee879704dbd74b485115402c3`, 570246 bytes | proven | Re-materialize and disclose the cache hash for each live paper-result run. |
| P8/AIME live operator path discloses profile, models, cache/resume, budget, baseline/optimized validation/test numbers, and deltas versus GEPA CAIS targets | upstream GEPA CAIS artifact and `examples/aime_math`; AIME spec sections 1-2 | P8 live OpenAI/runtime path | deterministic and cache-only proof classification tests; live config/cache reporting tests; current completed live report `.leaven/release-runs/p8-aime-gepa-current-release-20260518-094902-d2d15a36d364/reports/p8-aime.json` proves profile `gepa-aime`, `gepa_profile=reference`, proof class `full_live_aime_reproduction_attempt`, solver `gpt-4.1-mini`, reflection `gpt-5.4-mini`, materialized AIME cache SHA-256 `0f39c54861fd37a609d5bf397902a2086c245ebee879704dbd74b485115402c3`, source counts `45/45/30`, run id `ec039f2d-45b0-4cfa-8615-4d21dcdfbfda`, resumable run dir/checkpoint, live provider proof for both roles, zero process-local and durable provider failures, solver cache `hits=300 misses=382`, reflection cache `hits=21 misses=19`, search budget `530/500` with GEPA-style overshoot `30`, final-report metric calls `150`, baseline/optimized validation `0.444 -> 0.489`, baseline/optimized held-out test `0.433 -> 0.500`, `gepa_best_index=3`, `candidate_count=7`, `proposal_attempt_count=40`, `accepted_count=6`, `accepted_unadmitted_count=0`, `full_validation_evals=7`, candidate system-prompt projection, prompt-contract examples/fingerprints, and baseline/optimized per-case rows for train/validation/test final reports | proven | The current report is the release-proof artifact for operator path/report freshness. Older completed report `.leaven/release-runs/p8-aime-gepa-20260518-043717/reports/p8-aime.json` and failed JSON-fallback run `.leaven/release-runs/p8-aime-gepa-current-json-fallback-20260518-084304` remain diagnosis only. |
| P8/AIME live result quality improves over seed and is comparable to the GEPA CAIS target | upstream GEPA CAIS artifact and `examples/aime_math`; AIME spec sections 1-2 | P8 live OpenAI/runtime path plus emitted prompts/reflection/proposal traces | current completed live report `.leaven/release-runs/p8-aime-gepa-current-release-20260518-094902-d2d15a36d364/reports/p8-aime.json` shows seed improvement on validation `0.444 -> 0.489` and held-out test `0.433 -> 0.500`; stale completed report `.leaven/release-runs/p8-aime-gepa-20260518-043717/reports/p8-aime.json` shows `best_index=0`, validation `0.533 -> 0.533`, held-out test `0.433 -> 0.433`; older summary `.leaven/release-runs/p8-aime-gepa-20260516-221231/reports/summary.json` improved validation `0.444 -> 0.533` and held-out test `0.367 -> 0.467` but lacks current report schema/proof fields | gap | Current code now proves a live improvement over seed, but it is not comparable-to-target parity: held-out test `0.500` still trails the pinned GEPA CAIS artifact target `0.600`, and the report labels Leaven's reflection model delta (`gpt-5.4-mini` versus upstream `openai/gpt-5.1`). Continue quality diagnosis from emitted reflections/proposals and candidate lineage before claiming "as good as GEPA." |
| P8 model profile is deliberately Leaven-better, not byte-for-byte upstream AIME | upstream AIME uses `gpt-4.1-mini` solver and `openai/gpt-5.1` reflection; Leaven spec names `gpt-5.4-mini` reasoning delta | `AimeRunProfile`, P8 model/runtime config | `configured_gepa_aime_profile_matches_reference_knobs` covers budget/cache/model/runtime knobs; runtime report tests | intentional-delta | Every live report must label the model delta and compare against the upstream target rather than claiming identical runtime conditions. |
| P8/AIME optimize-anything profile does not silently inherit core GEPA defaults | upstream `src/gepa/optimize_anything.py::ReflectionConfig` defaults `skip_perfect_score=False`; `GEPAConfig.merge=None`; `examples/aime_math/main.py` overrides cache/budget/model only | P8 `run_aime` builds `Gepa::reflect_with_lm(...)` with `.skip_perfect_score(false)` and reports that value | `configured_gepa_aime_profile_matches_reference_knobs`; `deterministic_aime_acceptance_shows_public_gepa_improvement`; `p8_aime_json_report_persists_role_and_proof_facts` | proven | Keep core GEPA skip-perfect and optimize-anything/AIME skip-perfect as separate profile facts in reports. |
| DSPy default parity is not claimed until merge and DSPy trace/feedback defaults exist or are labeled disabled | upstream `dspy.GEPA` defaults `use_merge=True` and DSPy adapter trace/format-failure feedback | Leaven core GEPA and P8 optimize-anything profile | spec/report language; no merge implementation surfaced in GEPA | intentional-delta | Keep current claims labeled core GEPA or optimize-anything AIME, not DSPy-default parity. |

## Current Priority

The next implementation pass should not spend live provider budget by default.
The current operator/report row is proven; result-quality parity remains open.
Use the completed report's emitted prompts, proposal attempts, candidate
lineage, and case deltas to decide the next library-level quality/speed change.

1. Compare accepted/rejected Leaven reflections against pinned upstream
   GEPA/AIME traces or source behavior where available.
2. Decide whether the next experiment is a strict-reference larger-model run or
   a labeled Leaven-plus profile such as active failure sampling, side-info
   compression, serial multi-proposal diversity, or lazy certified racing.
3. Keep `GepaProfile::Reference` as the parity baseline. Any FastGEPA work must
   be an explicit non-parity profile until full-certification quality is proven.
