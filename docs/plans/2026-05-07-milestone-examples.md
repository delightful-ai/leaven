# Tombstone: Milestone Examples Implementation Plan

Status: completed and superseded for P0-P4.

This file used to be the implementation plan for turning P0-P4 milestone
examples from placeholders into executable checks. It is intentionally no
longer the source of truth.

Current owners:

- `docs/specs/milestone_examples_behavioral_contract.md` owns P0-P4
  behavioral requirements.
- `examples/AGENTS.md` owns proof classification for each example package.
- `docs/testing/README.md` owns milestone proof commands.
- `Justfile` owns the runnable `just milestone-pN` recipes.
- `crates/leaven/tests/topology_contract.rs` owns live topology and deleted
  placeholder-crate guards.

Do not use the old task list or deleted crate names here as current guidance.
