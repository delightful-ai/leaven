# generated by datamodel-codegen:
#   filename:  leaven.evaluation_job.v1.schema.json

from __future__ import annotations

from enum import Enum
from typing import Any, Literal

from pydantic import AwareDatetime, BaseModel, ConfigDict, Field, conint, constr

from . import common_schema


class Kind(BaseModel):
    model_config = ConfigDict(
        extra="forbid",
    )
    kind: Literal["independent"]
    candidates: list[common_schema.CandidateId | common_schema.CandidateRef1] = Field(
        ..., min_length=1
    )


class Pair(BaseModel):
    model_config = ConfigDict(
        extra="forbid",
    )
    left: common_schema.CandidateId | common_schema.CandidateRef1
    right: common_schema.CandidateId | common_schema.CandidateRef1


class Kind3(BaseModel):
    model_config = ConfigDict(
        extra="forbid",
    )
    kind: Literal["pairwise"]
    pairs: list[Pair] = Field(..., min_length=1)


class Kind4(BaseModel):
    model_config = ConfigDict(
        extra="forbid",
    )
    kind: Literal["listwise"]
    candidates: list[common_schema.CandidateId | common_schema.CandidateRef1] = Field(
        ..., min_length=1
    )


class Granularity(Enum):
    aggregate = "aggregate"
    per_case = "per_case"


class Purpose(Enum):
    train = "train"
    validation = "validation"
    test = "test"
    diagnostic = "diagnostic"
    custom = "custom"


class ResolvedSet(BaseModel):
    model_config = ConfigDict(
        extra="forbid",
    )
    id: constr(pattern=r"^rset_[A-Za-z0-9_.:-]+$")
    case_ids: list[common_schema.CaseId | common_schema.CaseRef1] | None = None
    case_count: conint(ge=0)
    case_cursor: constr(pattern=r"^cur_[A-Za-z0-9_.:-]+$") | None = None
    case_set_version: str | None = None
    partition_summary: dict[str, int] | None = None


class LeavenEvaluationJobV1V03(BaseModel):
    """
    Evaluator-specific invocation. Targets are handles; explicit case target reads cross case.load under evaluator capability.
    """

    model_config = ConfigDict(
        extra="forbid",
    )
    schema_version: Literal["leaven.evaluation_job.v1"]
    run: constr(pattern=r"^run_[A-Za-z0-9_.:-]+$")
    stage_call_id: constr(pattern=r"^sc_[A-Za-z0-9_.:-]+$")
    evaluation_attempt_id: constr(pattern=r"^evalatt_[A-Za-z0-9_.:-]+$") | None = None
    evaluation_request_id: constr(pattern=r"^evalreq_[A-Za-z0-9_.:-]+$")
    evaluator_id: str
    evaluator_fingerprint: constr(pattern=r"^fp_[a-z0-9_]+_[A-Za-z0-9_.:-]+$") | None = None
    base_revision: constr(pattern=r"^rev_[A-Za-z0-9_.:-]+$")
    deadline_at: AwareDatetime | None = None
    parent_job_id: str | None = None
    target_egress_policy_ref: constr(pattern=r"^fp_policy_sha256_[A-Za-z0-9_-]+$") | None = None
    kind: Kind | Kind3 | Kind4
    granularity: Granularity
    purpose: Purpose
    resolved_set: ResolvedSet
    capability_fingerprint: constr(pattern=r"^fp_cap_sha256_[A-Za-z0-9_-]+$")
    metadata: dict[str, Any] | None = None
