interface:
  display_name: "SDK Dependency Updates"
  short_description: "Review Braintrust SDK dependency PRs"
  default_prompt: "Use $sdk-dependency-updates to review this Braintrust SDK dependency PR, decide whether cassettes need refreshing, and validate the exact sessions to run."
