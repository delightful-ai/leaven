[project]
name = "braintrust-cohere-example"
version = "0.1.0"
description = "Cohere client traced with Braintrust via wrap_cohere"
requires-python = ">=3.10"
dependencies = [
    "braintrust",
    "cohere",
]

[tool.uv.sources]
braintrust = { path = "../../py", editable = true }
