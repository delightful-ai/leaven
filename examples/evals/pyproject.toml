[project]
name = "braintrust-evals-example"
version = "0.1.0"
description = "Two minimal Eval() examples for the Braintrust evaluation framework"
requires-python = ">=3.10"
dependencies = [
    "braintrust",
]

[tool.uv.sources]
braintrust = { path = "../../py", editable = true }
