[project]
name = "braintrust-crewai-example"
version = "0.1.0"
description = "CrewAI Agent + Task + Crew traced with Braintrust"
requires-python = ">=3.10"
dependencies = [
    "braintrust",
    "crewai",
]

[tool.uv.sources]
braintrust = { path = "../../py", editable = true }
