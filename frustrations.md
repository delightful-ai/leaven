# Tombstone: Frustrations

Status: superseded scratch note.

This root-level complaint log was useful while P5, app-server, and provider
runtime proof boundaries were unsettled. The durable lessons now live closer to
the owning surfaces:

- `examples/AGENTS.md` and example-local `AGENTS.md` files classify product
  proof, mechanics smoke, proxy demos, and live provider gates.
- `docs/working-memory/skill-paper-replication.md` and
  `docs/working-memory/evoskill-replication.md` track paper-replication gaps.
- `crates/leaven-agent*` and `crates/leaven-acp` local guidance own provider
  runtime and external-worker transport boundaries.

Do not use this old scratch file as current implementation status.
